import { NgxUiLoaderConfig, SPINNER } from 'ngx-ui-loader';

const ngxUiLoaderConfig: NgxUiLoaderConfig = {
    fgsType: SPINNER.ballScaleMultiple,
    overlayColor: 'rgba(40,40,40,0.2)',
    blur: 3,
    delay: 200,
    minTime: 0,
    hasProgressBar: false,
    fgsColor: "#0d47a1",
};

export { ngxUiLoaderConfig };
